1 npm init
2 npm install express --save установка экспресс

Axios — это JavaScript-библиотека для выполнения либо HTTP-запросов в Node.js, либо XMLHttpRequests в браузере.
Она поддерживает промисы — новинку ES6. Одна из особенностей, которая делает её лучше fetch() 
— автоматические преобразования JSON-данных.

npm install body-parser --save

npm install mongodb

 Переход на другой диск в командной виндовс cd /d d:\ переходим в папку с базой  cd mongodb запускаем ее mongod


npm install express-session --save
